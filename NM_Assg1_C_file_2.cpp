#include<iostream>
#include<conio.h>
#include<stdlib.h>
#include<float.h>

using namespace std;

int main()
{

	cout<<" \n The minimum possible value of char datatype is "<<CHAR_MIN<<" and the maximum possible value is "<<CHAR_MAX;
	cout<<" \n The minimum possible value of int datatype is "<<INT_MIN<<" and the maximum possible value is "<<INT_MAX;
	cout<<" \n The minimum possible value of long int datatype is "<<LONG_MIN<<" and the maximum possible value is "<<LONG_MAX;
	cout<<" \n The minimum possible value of float datatype is "<<FLT_MIN<<" and the maximum possible value is "<<FLT_MAX;
	cout<<" \n The minimum possible value of double datatype is "<<DBL_MIN<<" and the maximum possible value is "<<DBL_MAX;
	getch();
	return 0;
}
